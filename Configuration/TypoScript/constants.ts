
plugin.tx_pbosmpartners_pi1 {
  view {
    # cat=plugin.tx_pbosmpartners_pi1/file; type=string; label=Path to template root (FE)
    templateRootPath =
    # cat=plugin.tx_pbosmpartners_pi1/file; type=string; label=Path to template partials (FE)
    partialRootPath =
    # cat=plugin.tx_pbosmpartners_pi1/file; type=string; label=Path to template layouts (FE)
    layoutRootPath =
  }
  persistence {
    # cat=plugin.tx_pbosmpartners_pi1//a; type=string; label=Default storage PID
    storagePid =
  }
}
